builds:
  - binary: restify
    env:
      # to allow for alpine/musl use
      - CGO_ENABLED=0
    goos:
      - windows
      - darwin
      - linux
    goarch:
      - amd64
      - arm
changelog:
  filters:
    exclude:
      - '^build:'
      - '^ci:'
dockers:
  - image: itzg/restify